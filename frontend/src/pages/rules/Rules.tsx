import { Typography } from "@mui/material";
import React from "react";
import Wrapper from "../../components/wrapper/Wrapper";

export default function Rules(){
  return(
    <Wrapper paperComponent title="REGRAS">
      <Typography>Texto</Typography>
    </Wrapper>
  )
}