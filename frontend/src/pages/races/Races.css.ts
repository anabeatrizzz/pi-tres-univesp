const styles = {
  racesContainer: {
    display: "flex",
    justifyContent: "space-evenly",
    flexWrap: "wrap" as "wrap"
  },

  editIconDiv: {
    display: "flex",
    justifyContent: "flex-end"
  }
}

export default styles;